@extends('layouts.app')

@section('title', 'Project: Cured Leaves Tea | Amaya Shaw')

@section('content')
<main class="bg-white min-h-screen">
    <div class="max-w-6xl mx-auto py-24 px-6">
        
        {{-- BACK LINK --}}
        <nav class="mb-16 reveal">
            <a href="{{ route('graphic-design') }}" class="group inline-flex items-center text-[10px] font-bold uppercase tracking-[0.5em] text-gray-400 hover:text-sage-600 transition">
                <span class="mr-2 transform group-hover:-translate-x-2 transition-transform">←</span> Back to Gallery
            </a>
        </nav>

        {{-- HERO --}}
        <header class="mb-32 reveal">
            <span class="text-[10px] font-bold uppercase tracking-[0.5em] text-sage-600 mb-4 block">Visual Storytelling — 2024</span>
            <h1 class="text-6xl md:text-8xl font-black text-gray-900 mb-12 tracking-tighter uppercase leading-[0.8]">
                Cured <br><span class="text-gray-300">Leaves Tea.</span>
            </h1>

            {{-- CLT STAT BAR --}}
            <div class="pt-10 border-t border-gray-100 grid grid-cols-2 lg:grid-cols-4 gap-12">
                <div>
                    <h4 class="text-[10px] font-bold uppercase tracking-widest text-gray-300 mb-3">Role</h4>
                    <p class="text-xs md:text-sm font-bold text-gray-700 uppercase tracking-tight">Content Lead</p>
                </div>
                <div>
                    <h4 class="text-[10px] font-bold uppercase tracking-widest text-gray-300 mb-3">Focus</h4>
                    <p class="text-xs md:text-sm font-bold text-gray-700 uppercase tracking-tight">Digital Strategy</p>
                </div>
                <div>
                    <h4 class="text-[10px] font-bold uppercase tracking-widest text-gray-300 mb-3">Vibe</h4>
                    <p class="text-xs md:text-sm font-bold text-sage-600 uppercase tracking-tight italic">Organic & Bold</p>
                </div>
                <div>
                    <h4 class="text-[10px] font-bold uppercase tracking-widest text-gray-300 mb-3">Platform</h4>
                    <p class="text-xs md:text-sm font-bold text-gray-700 uppercase tracking-tight">Instagram / TikTok</p>
                </div>
            </div>
        </header>

        {{-- THE GRID --}}
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-40">
            @for ($i = 1; $i <= 6; $i++)
                <div class="reveal">
                    <div class="group bg-white rounded-[40px] shadow-sm overflow-hidden border border-gray-100 transition-all duration-700 hover:shadow-2xl">
                        <img src="{{ asset('photos/clt/clt1/' . $i . '.jpg') }}" class="w-full aspect-square object-cover transition-transform duration-700 group-hover:scale-110" alt="CLT Square {{ $i }}">
                    </div>
                </div>
            @endfor
        </div>

        {{-- QUOTE SECTION --}}
        <section class="reveal bg-sage-50/30 py-32 my-40 rounded-[60px] border border-sage-100">
            <div class="text-center px-6 max-w-4xl mx-auto">
                <span class="text-[10px] font-black uppercase tracking-[0.4em] text-sage-600 mb-8 block">The Philosophy</span>
                <h2 class="text-4xl md:text-6xl font-black text-gray-900 leading-[0.9] uppercase tracking-tighter">
                    "I create based on the <span class="text-sage-600 italic">visual heartbeat</span> of every brand."
                </h2>
            </div>
        </section>

        {{-- PORTRAIT VIDEO GRID --}}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {{-- Videos and Portraits here... --}}
        </div>
    </div>
</main>
@endsection